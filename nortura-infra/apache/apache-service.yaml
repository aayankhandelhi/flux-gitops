apiVersion: v1
kind: Service
metadata:
  name: apache-app-nodeport
  namespace: minside
  labels:
    app: apache-app
spec:
  type: NodePort
  selector:
    app: apache-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30081